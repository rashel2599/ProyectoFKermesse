@model IEnumerable<ProyectoFinalKermesse.Models.Denominacion>

@{
    ViewBag.Title = "VerReporteDenominacion";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>VerReporteDenominacion</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.valor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.valorLetras)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Moneda1.nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.valor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.valorLetras)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Moneda1.nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idDenominacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.idDenominacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idDenominacion })
        </td>
    </tr>
}

</table>
