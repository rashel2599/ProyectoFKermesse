@model IEnumerable<ProyectoFinalKermesse.Models.ControlBono>

@{
    ViewBag.Title = "VerReporteControlBonoDetalle";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>VerReporteControlBonoDetalle</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.valor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.valor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idBono }) |
            @Html.ActionLink("Details", "Details", new { id=item.idBono }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idBono })
        </td>
    </tr>
}

</table>
